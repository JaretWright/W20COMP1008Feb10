public abstract class Shape {

    /**
     * This is an abstract method.  That means it is NOT implemented
     * in this class, but the sub class must have this method
     * signature and it would implement the logic required to
     * calculate the area
     * @return
     */
    public abstract double getArea();
}
